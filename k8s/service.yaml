apiVersion: v1
kind: Service
metadata:
  name: wisecow-service
spec:
  selector:
    app: wisecow
  ports:
    - protocol: TCP
      port: 80 # Is port par hum traffic lenge
      targetPort: 4499 # App ka container port
  type: NodePort